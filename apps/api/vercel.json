{
  "buildCommand": "npx prisma generate && npm run build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index.ts" }
  ],
  "functions": {
    "api/index.ts": {
      "runtime": "nodejs20.x",
      "maxDuration": 10,
      "memory": 1024
    }
  }
}
